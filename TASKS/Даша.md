# Задачи Даши (Frontend junior)

## Задача: собрать карту мест, где еще используются mock-данные
Зачем нужна: чтобы команда видела, что осталось перевести на API.
Что обязательно прочитать:
- текущие страницы в `frontend/app/(app)/*`
- текущий `frontend/lib/store.ts`
Где в коде работать:
- документация/заметка по результатам
Что должно получиться на выходе:
- список экранов и действий, которые еще используют mock CRUD.
Зависит от: нет.
Можно делать параллельно: со всеми стартовыми задачами команды.
Крайний срок: 5 мая 2026.

## Задача: доработать состояния форм входа и регистрации
Зачем нужна: это первый экран пользователя, ошибки должны быть понятными.
Что обязательно прочитать:
- `docs/api/auth.md`
- `docs/api/errors.md`
Где в коде работать:
- `frontend/app/(auth)/login/page.tsx`
- `frontend/app/(auth)/register/page.tsx`
Что должно получиться на выходе:
- корректные loading/error-состояния, валидация длины пароля, понятные сообщения.
Зависит от: от готовности auth endpoint-ов для финальной проверки.
Можно делать параллельно: с backend auth реализацией и с API-клиентом Маши.
Крайний срок: 6 мая 2026.

## Задача: добавить и проверить UI-состояния основных экранов
Зачем нужна: пользователь должен понимать, что происходит при загрузке, пустом результате и ошибке.
Что обязательно прочитать:
- `TASKS/Общий_бэклог.md`
Где в коде работать:
- `/projects`, `/tasks`, `/files`, overlay search
Что должно получиться на выходе:
- устойчивые loading/empty/error состояния на ключевых экранах.
Зависит от: от поэтапного подключения экранов к API.
Можно делать параллельно: с доработками Маши по тем же экранам.
Крайний срок: 14 мая 2026.

## Задача: провести ручную регрессию перед демо
Зачем нужна: поймать баги, которые не увидит автоматическая проверка.
Что обязательно прочитать:
- `TASKS/Общий_бэклог.md`
Где в коде работать:
- ручной прогон сценариев в приложении
Что должно получиться на выходе:
- список багов с шагами воспроизведения, ожиданием и фактическим результатом.
Зависит от: от готовности основного функционала на backend и frontend.
Можно делать параллельно: с backend smoke и Electron финальным прогоном.
Крайний срок: 15 мая 2026.
