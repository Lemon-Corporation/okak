# Задачи Андрея (Backend)

## Задача: реализовать модели задач, файлов, тегов и связующие таблицы
Зачем нужна: без этих сущностей невозможно закрыть основной функционал MVP.
Что обязательно прочитать:
- `docs/db/schema.md`
Где в коде работать:
- `backend/app/models/task.py`
- новые модели файлов и тегов
- модели junction-таблиц
Что должно получиться на выходе:
- модели и уникальные ограничения соответствуют схеме в docs.
Зависит от: от общего SQLAlchemy каркаса в backend.
Можно делать параллельно: с задачами Кости по `users/projects/notes` моделям.
Крайний срок: 5 мая 2026.

## Задача: внедрить единый формат ошибок backend
Зачем нужна: frontend должен получать предсказуемую структуру ошибок.
Что обязательно прочитать:
- `docs/api/errors.md`
Где в коде работать:
- общий слой ошибок backend
- endpoint-ы, где выбрасываются ручные ошибки
Что должно получиться на выходе:
- ручные ошибки возвращают `{ detail, code }`.
Зависит от: нет.
Можно делать параллельно: почти со всеми backend задачами.
Крайний срок: 6 мая 2026.

## Задача: реализовать теги и привязку тегов к заметкам
Зачем нужна: это часть базового UX заметок.
Что обязательно прочитать:
- `docs/api/tags.md`
- `docs/api/notes.md`
Где в коде работать:
- endpoint-ы тегов
- endpoint-ы привязки тегов
Что должно получиться на выходе:
- CRUD тегов, attach/detach, обработка дублей через `already_exists`.
Зависит от: от готовых моделей tags и note_tags, от auth.
Можно делать параллельно: с реализацией задач и поиском.
Крайний срок: 8 мая 2026.

## Задача: реализовать задачи по полному контракту
Зачем нужна: текущая логика `done` недостаточна, нужна модель со статусами/приоритетами/дедлайнами.
Что обязательно прочитать:
- `docs/api/tasks.md`
Где в коде работать:
- `backend/app/api/v1/tasks.py`
- сервисы задач
Что должно получиться на выходе:
- CRUD задач и переходы статусов работают по таблице логики из docs.
Зависит от: от готовых моделей tasks и access-check через project ownership.
Можно делать параллельно: с файлами и поиском.
Крайний срок: 11 мая 2026.

## Задача: реализовать файловый контур
Зачем нужна: загрузка и скачивание файлов — ключевой пользовательский сценарий.
Что обязательно прочитать:
- `docs/api/files.md`
Где в коде работать:
- `backend/app/api/v1/files.py`
- файловый сервис и хранилище
Что должно получиться на выходе:
- upload/download/delete работают, проверяются типы/размеры, есть checksum-проверка.
Зависит от: от моделей files и project ownership checks.
Можно делать параллельно: с задачами и тегами; frontend может начать UI upload states заранее.
Крайний срок: 13 мая 2026.

## Задача: реализовать глобальный поиск
Зачем нужна: overlay и быстрый доступ завязаны на поиске.
Что обязательно прочитать:
- `docs/api/search.md`
Где в коде работать:
- новый endpoint поиска
- SQL/ORM-слой поиска
Что должно получиться на выходе:
- `/search` возвращает проекты/заметки/задачи текущего пользователя.
Зависит от: от частичной готовности projects/notes/tasks данных.
Можно делать параллельно: с frontend overlay UI до момента подключения API.
Крайний срок: 13 мая 2026.

## Задача: подготовить smoke-проверки backend
Зачем нужна: быстро ловить критические поломки перед финалом.
Что обязательно прочитать:
- `TASKS/Общий_бэклог.md`
Где в коде работать:
- test/smoke сценарии
Что должно получиться на выходе:
- есть минимальный список проверок, который команда может прогнать перед демо.
Зависит от: от готовности всех критичных endpoint-ов.
Можно делать параллельно: с ручной frontend регрессией.
Крайний срок: 14 мая 2026.
