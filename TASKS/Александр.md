# Задачи Саши (Electron)

## Задача: выбрать и зафиксировать архитектуру desktop-слоя
Зачем нужна: без этого команда не поймет, где живет Electron и как его запускать.
Что обязательно прочитать:
- `TASKS/README.md`
- `TASKS/Общий_бэклог.md`
Где в коде работать:
- папка `desktop/` или интеграция рядом с `frontend/`
Что должно получиться на выходе:
- принятое решение по структуре, dev/prod flow описан в понятном виде.
Зависит от: нет.
Можно делать параллельно: с backend моделями, auth и frontend API-клиентом.
Крайний срок: 5 мая 2026.

## Задача: поднять рабочий dev-режим Electron
Зачем нужна: команда должна ежедневно проверять продукт в desktop-среде.
Что обязательно прочитать:
- текущий сценарий запуска frontend/backend
Где в коде работать:
- main/preload конфиг Electron
Что должно получиться на выходе:
- Electron открывает приложение, безопасные настройки включены (`contextIsolation`, отключенный `nodeIntegration`).
Зависит от: от поднятого frontend dev-сервера.
Можно делать параллельно: с backend API реализацией.
Крайний срок: 7 мая 2026.

## Задача: привести desktop UX к внятному виду
Зачем нужна: приложение должно ощущаться как продукт, а не тестовое окно.
Что обязательно прочитать:
- текущий UI сценарий продукта
Где в коде работать:
- окно, меню, иконка, shortcuts
Что должно получиться на выходе:
- имя приложения, иконка (хотя бы временная), корректные размеры окна, рабочие системные действия.
Зависит от: от рабочего dev-режима Electron.
Можно делать параллельно: с frontend интеграцией API.
Крайний срок: 10 мая 2026.

## Задача: настроить параметры desktop-окружения
Зачем нужна: нужно управлять backend URL и поведением desktop-версии.
Что обязательно прочитать:
- текущий frontend settings поток
Где в коде работать:
- desktop-конфиг и настройки приложения
Что должно получиться на выходе:
- понятный способ задавать API URL и запускать приложение в нужном режиме.
Зависит от: от базового каркаса Electron.
Можно делать параллельно: с backend задачами и AI интеграцией.
Крайний срок: 12 мая 2026.

## Задача: проверить файловые сценарии внутри Electron
Зачем нужна: upload/download часто ломаются в desktop-оболочке.
Что обязательно прочитать:
- `docs/api/files.md`
Где в коде работать:
- интеграция Electron + frontend files screen
Что должно получиться на выходе:
- файл загружается и скачивается в desktop-версии без критических ошибок.
Зависит от: от готовых backend files endpoint-ов и frontend files-интеграции.
Можно делать параллельно: с backend smoke и frontend регрессией.
Крайний срок: 13 мая 2026.

## Задача: подготовить production-сборку и демо
Зачем нужна: к финалу нужен либо реальный build, либо честно описанный блокер.
Что обязательно прочитать:
- `TASKS/Общий_бэклог.md`
Где в коде работать:
- сборка Electron (`builder`/`forge`) и скрипты запуска
Что должно получиться на выходе:
- production build или документированный блокер + инструкция запуска и known issues.
Зависит от: от стабильных frontend/backend сценариев.
Можно делать параллельно: с финальной документацией AI и API-контрактной сверкой.
Крайний срок: 15 мая 2026.
